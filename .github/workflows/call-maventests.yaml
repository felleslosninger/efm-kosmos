name: Run maven tests

on:
  push:
    branches: [main]

jobs:
  call-workflow-maven-build:
    uses: felleslosninger/github-workflows/.github/workflows/ci-maven-build.yml@eskwor-GH-TOKEN-permission
    with:
      java-version: 21
      maven-lifecycle: verify
    secrets: inherit
